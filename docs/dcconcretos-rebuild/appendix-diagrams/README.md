# Diagram appendix

- `architecture.mmd` — client ↔ SQL Server integration view
- `batch-flows.mmd` — batch forms ↔ I/O ↔ `Modelo.dll` ↔ database

Render with Mermaid CLI, VS Code Mermaid extension, or paste into GitHub/GitLab preview.
