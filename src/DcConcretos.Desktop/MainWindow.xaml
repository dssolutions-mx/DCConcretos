<Window x:Class="DcConcretos.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DC Concretos — Comercial"
        Height="520" Width="960"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="Cargas batch">
                <MenuItem x:Name="MenuCompras" Header="Compras…" Click="MenuCompras_OnClick"/>
                <MenuItem x:Name="MenuDiesel" Header="Diesel…" Click="MenuDiesel_OnClick"/>
                <MenuItem x:Name="MenuRemisiones" Header="Remisiones…" Click="MenuRemisiones_OnClick"/>
            </MenuItem>
        </Menu>
        <Grid Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Desde" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker x:Name="DpDesde" Width="140"/>
                <TextBlock Text="Hasta" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <DatePicker x:Name="DpHasta" Width="140"/>
                <Button x:Name="BtnRun" Content="Ejecutar reporte" Margin="16,0,0,0" Padding="12,4" Click="BtnRun_OnClick"/>
                <Button x:Name="BtnExport" Content="Exportar Excel…" Margin="8,0,0,0" Padding="12,4" Click="BtnExport_OnClick" IsEnabled="False"/>
            </StackPanel>
            <TextBlock Grid.Row="1" Text="Códigos de concepto (formato archivo ConceptosEstadoCuentaProveedor.txt):" Margin="0,0,0,4"/>
            <TextBox x:Name="TxtConceptos" Grid.Row="2" AcceptsReturn="True" Height="100" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap" FontFamily="Consolas" Margin="0,0,0,8"/>
            <DataGrid x:Name="GridResultados" Grid.Row="3" IsReadOnly="True" AutoGenerateColumns="True"/>
        </Grid>
    </DockPanel>
</Window>
